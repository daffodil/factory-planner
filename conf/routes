# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


module:testrunner

# GET     /                                       App.Index
GET     /jmobile                                JMobile.Index
GET     /jmobile/:page                          JMobile.Pages

## Accounts = Now in MOd above
# GET     /jmobile/accounts                       Account.JMobileIndex

GET      /ajax/accounts                           Account.JsonIndex



GET      /ajax/calendar/week/:view                Calendar.JsonWeek
GET      /ajax/calendar/weeks                     Calendar.JsonWeeks

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")



# Catch all
#*       /:controller/:action                    :controller.:action
